# Smart-Room-Monitor
A real-time smart room monitoring system using Arduino, STM32, and Python. Reads live sensor data (light, flame, sound), visualizes it in a PyQt GUI, and triggers hardware warning LEDs via UART communication.
